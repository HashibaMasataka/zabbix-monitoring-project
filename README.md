# 文化祭 NOCプロジェクト: Zabbix 7.0 LTS 監視サーバー構築

文化祭の校内ネットワーク（Wi-Fi・各種サーバー）を一元管理するため、プロキシ・仮想化環境（Proxmox）上に最新の **Zabbix 7.0 LTS** を用いたネットワークオペレーションセンター（NOC）の監視基盤を構築しました。

単にインストールするだけでなく、ローカル環境におけるセキュリティリスクの排除や、構築中に発生したインフラ特有のトラブルをログベースで解決したプロセスを記録しています。

---

## 🛠 構築環境
- **Hypervisor:** Proxmox VE
- **OS:** Ubuntu 24.04 LTS (Noble Numbat)
- **Monitoring Platform:** Zabbix 7.0 LTS (最新長期サポート版)
- **Database:** MariaDB 10.11

---

##  構築手順
本プロジェクトでは、インフラのコード化（IaC）を意識し、クリーンなUbuntu環境から一撃でHTTPS化まで完了するシェルスクリプトを作成しました。

実行コードの詳細は、本リポジトリ内の `setup.sh` を参照してください。

---

##  構築中に直面したトラブルと解決プロセス

実際のプロダクション環境と同様に発生したインフラトラブルに対し、ログの監視と仮説検証を基に以下の通り解決しました。

### 1. ディスク容量不足（インフラパニック）によるフリーズ
- **事象:** 初期割り当て容量（2.4GB）が100%になり、OSおよびパッケージ展開が完全停止。
- **原因:** OSのログおよびZabbixのファイル展開によるバースト。
- **対策:** `/tmp` 領域を一時的にメモリ上に逃がしてコマンド受付を回復させた後、Proxmox側でストレージを **22GBへ拡張**。Linuxのパーティションをリサイズしデータを保持したまま復旧。

### 2. パッケージの不完全インストール（ファイル欠損）の特定
- **事象:** 容量復旧後、設定書き込みコマンド（`sed`）が `No such file or directory` でエラー終了。
- **原因:** 1の容量不足時に、Zabbixサーバーのコアパッケージ（`zabbix_server.conf` 等）の解凍が途中で強制終了していた。
- **対策:** `find` コマンドで設定ファイルの存在を検索し、未インストール状態を特定。`apt-get install` を再実行して依存関係を正常に修復。

### 3. CUI（ターミナル）の改行バグによるSQLインポート失敗
- **事象:** データベースへの初期データ（数万行のスキーマ）流し込み（`zcat`）がエラー終了。
- **原因:** ターミナルへの一括貼り付け時、改行コードのズレによりコマンドが途中で誤認識された。
- **対策:** ファイルの絶対パスを確認し、対話型ではなく単発のインポートコマンドとして正確に再実行し完了。

---

## セキュリティ対策：ローカル環境のHTTPS化

初期状態のHTTP通信では、文化祭の来場者（悪意あるユーザー）にネットワーク上で管理用パスワード（スニフィング等）を盗聴されるリスクがありました。
本環境は校内LANの閉じられた空間であるため、外部の認証機関（Let's Encrypt等）ではなく、**OpenSSLを用いた自己署名証明書（有効期限10年）**を自作。

Apacheの仮想ホスト設定（`default-ssl.conf`）を書き換え、Zabbix管理画面への通信を完全に暗号化（HTTPS）しました。

---

## 構築完了ステータス

無事にすべてのバックエンドサービスが正常起動し、日本の標準時（JST / 東京タイムゾーン）に同期された監視ダッシュボードが立ち上がりました。

### ダッシュボード画面（グローバルビュー）
![Zabbix 7.0 Dashboard](./images/01_dashboard.png)

- Zabbixサーバー：**稼働中（はい）**
- フロントエンドバージョン：**7.0.26 (最新)**

---

##  今後の展望（TODO）
- [ ] **DHCPサーバー（コンテナ）の監視:** Zabbix Agent 2 を導入し、IPアドレス枯渇によるWi-Fi接続不能リスクを検知する。
- [ ] **ネットワーク機器のSNMP監視:** コアスイッチや各フロアのAPからトラフィック情報を取得し、接続人数をグラフ化する。
- [ ] **ダッシュボードの最適化:** 初期設定のジオマップ（世界地図）を排除し、校内マップやリアルタイム障害一覧パネルへカスタマイズする。
